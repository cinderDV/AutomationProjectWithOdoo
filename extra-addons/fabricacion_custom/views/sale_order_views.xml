<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_sale_order_form_inherit_delay_tracking" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.delay.tracking</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <!-- Agregar nueva página para seguimiento de retrasos -->
            <xpath expr="//notebook" position="inside">
                <page string="Seguimiento de Retrasos" name="delay_tracking">
                    <group>
                        <group>
                            <field name="x_external_order_id"/>
                            <field name="x_delay_days"/>
                        </group>
                        <group>
                            <field name="x_delay_status" widget="badge"/>
                            <field name="x_stuck_area"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
